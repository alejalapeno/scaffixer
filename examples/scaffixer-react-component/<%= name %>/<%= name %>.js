import * as React from 'react';
import PropTypes from 'prop-types';
import styles from './<%= Name %>.module.scss';

const <%= Name %> = () => {
	return (
		<>
		</>
	);
}

<%= Name %>.propTypes = {
};

export default <%= Name %>;
